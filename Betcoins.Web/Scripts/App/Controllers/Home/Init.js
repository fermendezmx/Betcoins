angular
    .module('betcoins.home.init', [])
    .controller('homeInitController', [
        '$rootScope',
        function ($rootScope) {
            //#region Init

            //#endregion
        }
    ]);